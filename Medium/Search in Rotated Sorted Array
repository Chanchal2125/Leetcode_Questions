class Solution {
public:
    int search(vector<int>& nums, int target) {
        int m = nums.size();
        for(int i = 0; i < m; i++){
            if(nums[i] == target) return i;
        }
        return -1;
    }
};
